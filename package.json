{
  "name": "shlvd",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "deploy": "git pull && npm run build && firebase deploy --only hosting"
  },
  "dependencies": {
    "firebase": "^10.14.0"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
